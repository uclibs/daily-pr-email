name: Daily PR Email

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes (testing)
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Build digest markdown file
        env:
          REPOS: "uclibs/staff-directory-23"
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y jq

          now="$(date -u '+%Y-%m-%d %H:%M UTC')"

          {
            echo "## PR Digest (${now})"
            echo
            echo "_Open PRs; excluding drafts; excluding titles starting with **WIP**._"
            echo
          } > digest.md

          for r in $REPOS; do
            prs_json=$(gh pr list -R "$r" --state open --json title,url,isDraft)

            filtered=$(echo "$prs_json" | jq '
              [ .[]
                | select(.isDraft == false)
                | select((.title | ascii_downcase | startswith("wip")) | not)
              ]')

            count=$(echo "$filtered" | jq 'length')

            {
              echo "### ${r}"
              if [ "$count" -eq 0 ]; then
                echo "- (no open PRs)"
              else
                echo "$filtered" | jq -r '.[] | "- [\(.title)](\(.url))"'
              fi
              echo
            } >> digest.md
          done

      - name: Create a new issue (each run)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          title="PR Digest TEST - run $GITHUB_RUN_ID"
          gh issue create \
            --repo "$GITHUB_REPOSITORY" \
            --title "$title" \
            --body-file digest.md \
            --label "pr-digest"